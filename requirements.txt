numpy<2                # torch 2.2.x is built against NumPy 1.x; NumPy 2 crashes encode()
sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Ingestion + chunking (Milestone 3)
requests>=2.32.0                  # fetching web sources
beautifulsoup4>=4.12.0            # HTML extraction / cleaning
langchain-text-splitters>=0.3.0   # RecursiveCharacterTextSplitter

# Query interface (Milestone 5)
gradio>=4.0          # 4.44.1 is the newest Gradio that supports Python 3.9

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
